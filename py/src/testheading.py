from bbsengine6 import util

buf = "projup #8237 - fix off-by-one glitch in bbsengine6.util.heading()"
print(len(buf))
util.heading(buf)
buf = "!projup #8237 - fix off-by-one glitch in bbsengine6.util.heading()"
print(len(buf))
util.heading(buf)
