> **STATUS (2026-07-22): OBSOLETE.** `runsubmodule()` was
> removed from the bbsengine6 module API; see
> `handbook/module.md` and `handbook/specs/module.md` for the
> current API. This file is preserved for historical reference
> only.

@since 20230205

submodules
===========

API
----
runcallback()
runmodule()
runsubmodule()

- used by empyre, murder motel, etc
- dynamically loads programs at run time
- saves memory

